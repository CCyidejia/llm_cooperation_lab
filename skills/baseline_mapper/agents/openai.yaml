interface:
  display_name: "Baseline Mapper"
  short_description: "Map mechanisms to PG, PD, or trust baselines"
  default_prompt: "Use $baseline-mapper to map this mechanism onto the supplied game baseline while preserving its invariants."
